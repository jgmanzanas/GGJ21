using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class CambiarMenuses : MonoBehaviour
{

    // Update is called once per frame
    public void Menuses()
    {
        SceneManager.LoadScene(1);
    }
}
